define_year_month = function(yearB,monthB) {
  year <<- yearB # Enters 'yearB' as called in the function into the R global environment
  month <<- monthB # Enters 'monthB' as called in the function into the R global environment
  }
